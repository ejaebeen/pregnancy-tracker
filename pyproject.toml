[project]
name = "jl-pregnancy-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ejaebeen", email = "ejaebeen@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
jl-pregnancy-tracker = "jl_pregnancy_tracker:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
